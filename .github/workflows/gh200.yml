name: CoHack Journey
on: push

jobs:
  hello-world:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: echo "Hello from GH Actions Session"

      - name: Run Shell Script
        run: |
          ./scripts/greet.sh
